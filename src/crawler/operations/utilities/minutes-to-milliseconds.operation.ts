/**
 * Converts minutes to milliseconds
 * @param minutes Time in minutes
 * @returns Time in milliseconds
 */
export function minutesToMillisecondsOperation(minutes: number): number {
    return minutes * 60 * 1000;
}


